# express.static
It is a middleware function that allows you to serve static assets (css files, images, js files) directly to the client, without needing to manually create routes for each file.

Environment Variable: The environment variable port should be defined in your .env file without any trailing characters. 